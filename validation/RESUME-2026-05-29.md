# RESUME — 2026-05-29 (post-compaction handoff)

Read this top-to-bottom before resuming. Repo: `spec-kit-linear`, local `/Users/ashbrener/Code/AI/speckit-linear`, GitHub `ashbrener/spec-kit-linear` (**PUBLIC**).

## Git / push mechanics (IMPORTANT)

- Default remote is **SSH with no key** → SSH push/fetch FAILS. Always use HTTPS-via-gh:
  `git -c credential.helper='!gh auth git-credential' push https://github.com/ashbrener/spec-kit-linear.git <branch>`
  (same pattern for fetch).
- `main` is protected → land changes via PR (`gh pr merge N --squash --delete-branch`). The operator has been doing merges; admin-merge worked for an urgent one.
- Commit messages: NO AI-attribution / Co-Authored-By trailers (operator's global rule). PR bodies may keep the "Generated with Claude Code" footer.

## Releases shipped this session

- **v0.1.0** — initial bridge (spec 001)
- **v0.1.1** — install ergonomics (spec 002)
- **v0.1.2** — worktree-hooks fix (#14) + merge-detection fix (#15); constitution v2.0.0 governance (#13)
- Constitution amended **v1.0.0 → v2.0.0**: Principle IV redefined "Write-Authority Follows The Filesystem (Drift-Aware)".

## Current branch/PR state (origin)

- `main` @ v0.1.2 (`fb98224`-ish tip). Clean.
- **PR #22** — `003-drift-aware-authority` (v0.2.0), spec 003 fully implemented (Phases 1-6, 56 tasks T300-T355). 5/6 CI checks green; **`bats (macos-latest, bash 5.2)` FAILS**. Branch tip `e937f23`.
- **PR #21** — `docs/design-question-linearrc` (Q2 doc). MERGEABLE/CLEAN, just needs merge.
- Issues open: **#17** (spec→Project design Q), **#20** (.linearrc cascade design Q). Both inert "DO NOT IMPLEMENT" parking-lot items, also in `validation/design-questions.md`.

## THREE OPEN ITEMS to finish (the resume work)

### 1. macOS CI failure on PR #22  (unblocks the spec 003 merge)
- 4 tests in `tests/unit/drift_disposition.bats` fail on macOS with `timeout: command not found` (GNU coreutils `timeout` absent on macOS; ubuntu passes).
- Failing tests: US2/T334 (~line 260), US2/T329 (~line 270), US3/T343 EOF + tty-seam (~lines 92-93).
- FIX: make the timeout-guard portable — detect `timeout`/`gtimeout` else a bash-native `_with_timeout` wrapper (background pid + sleep-kill watcher). Preserve the no-hang assertion (SC-019 / A10: prompt reads `/dev/tty`, EOF→safe-abort). Touch ONLY `drift_disposition.bats`.
- A stopped agent (`a8ea08aea11f94a31`) was mid-fix in worktree `.claude/worktrees/agent-a8ea08aea11f94a31` — partial, uncommitted. Discard + redo cleanly, or salvage.

### 2. Identifier / PII scrub  (public-repo privacy — HIGH)
Real values leak across tracked files. Replacements:
| Real (REMOVE) | Replace |
|---|---|
| Team UUID `6ab43461-6d22-4f02-bb1e-0be9859c7997` | `11111111-1111-4111-8111-111111111111` |
| Project UUID `dc2e7503-4b65-42ac-bed8-d2aa2d817f60` | `22222222-2222-4222-8222-222222222222` |
| Operator user_id `9f411c68-640a-4f80-a803-c8716caff3f0` | `33333333-3333-4333-8333-333333333333` |
| Operator email `ash@starlogik.com` (**PII**) | `operator@example.com` |
| Workspace `OSH-INFRA` / slug `osh-infra` | `ACME` / `acme` |
| Issue ids `OSH-<N>` (1..22) | `ACM-<N>` |
- WORST leak: `.specify/extensions/linear/linear-config.yml` is **committed** (real UUIDs + email + workspace). Fix: `git rm --cached` it + add to `.gitignore` (keep local file on disk for dogfooding); `config-template.yml` stays as the placeholder template (verify it has no real values).
- Also in: `scripts/dogfood.sh` (real Team UUID default + OSH-INFRA + OSH-N), `tests/fixtures/linear_responses/*.json`, `tests/unit/install_discovery.bats`, `tests/unit/install_backwards_compat.bats`, `tests/integration/us3-pull-cross-repo.bats`, `validation/{dogfood-001,handoff-2026-05-28-canonical-rebuild,linear-workspace-probe}.md`, several `specs/001`+`specs/002` contracts/docs, `CHANGELOG.md`, `commands/linear-pull.md`, `commands/linear-status.md`, `config-template.yml`.
- Add a guard: `tests/unit/no-real-identifiers.bats` that greps the tree + fails if any real string reappears (construct patterns so the guard file isn't a self-match).
- Verify: `git grep -iE '6ab43461|dc2e7503|9f411c68|starlogik|OSH-INFRA|osh-infra|OSH-[0-9]'` → 0 matches.
- ✅ Real API key NOT committed (only `lin_api_fake`-style test fakes) — no credential leak.
- A stopped agent (`a876fbd3682598a8d`) was mid-scrub in worktree `.claude/worktrees/agent-a876fbd3682598a8d` — partial, uncommitted. Discard + redo cleanly.
- HISTORY CAVEAT (operator decision, not auto): the real `linear-config.yml` (email + UUIDs) is in git HISTORY since spec 001. `git rm --cached` only fixes it going forward. Full purge = `git filter-repo` + **force-push to public main** — risky; operator must explicitly authorize. They were weighing this for the PII.

### 3. v0.2.0 release (gated)
- After PR #22 is green (item 1) + merged: tag **v0.2.0** — but T355 gates the tag on the **operator-run downstream dogfood** (`validation/dogfood-003.md`, an OPERATOR-PENDING checklist: SC-014 merged-from-main / SC-015 retroactive converge / SC-017 no spurious drift). That live run is on the operator's downstream consumer repo (not reconcilable from this repo). Tag follows their confirmation.

## Cleanup needed first thing on resume

Two stopped-agent worktrees linger (partial, uncommitted, safe to discard):
```
git worktree remove --force .claude/worktrees/agent-a876fbd3682598a8d
git worktree remove --force .claude/worktrees/agent-a8ea08aea11f94a31
git worktree prune
git branch -D worktree-agent-a876fbd3682598a8d worktree-agent-a8ea08aea11f94a31 2>/dev/null
```
(Unlock first if needed: `git worktree unlock <path>`.)

## Agent-stall lesson (operational)

Background agents repeatedly hit the 600s stream watchdog on this branch's later phases. Pattern: work lands on disk in the worktree, agent dies before commit. RESCUE = verify (lint+bats) → tick tasks.md → commit on its behalf → push. For the two remaining items, prefer **main-thread or tightly-scoped single-purpose agents** (worktree-isolated). Every code-writing agent MUST use `isolation: worktree` — a non-worktree agent earlier thrashed the shared checkout (lost an edit, polluted a PR base).

## Resume prompt (paste after compaction)

> Resuming spec-kit-linear. Read `validation/RESUME-2026-05-29.md` first. Then: (1) discard the two stopped-agent worktrees per the cleanup block; (2) fix the macOS `timeout` failure in `tests/unit/drift_disposition.bats` on the `003-drift-aware-authority` branch (portable timeout wrapper) → push → PR #22 goes 6/6 green; (3) do the identifier/PII scrub per the table (untrack linear-config.yml + genericize + guard test) as its own PR to main; (4) surface both for review. Hold the v0.2.0 tag for the operator's dogfood-003. Use HTTPS-via-gh for all pushes; worktree-isolate any agents.
